name: tick
on:
  schedule:
    - cron: "7,37 * * * *"   # 2 attempts/hour — GH cron is best-effort, over-provision
  workflow_dispatch: {}

concurrency:
  group: tick
  cancel-in-progress: false

jobs:
  tick:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -q "psycopg[binary]" requests google-cloud-firestore sentry-sdk
      - name: Run tick
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
        run: python -m jobs.tick
      - name: Setup SSH keys
        env:
          TRADER_SSH_KEY: ${{ secrets.TRADER_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$TRADER_SSH_KEY" > ~/.ssh/trader_key && chmod 600 ~/.ssh/trader_key
          ssh-keyscan github.com >> ~/.ssh/known_hosts 2>/dev/null
      - name: Clone trader-arena (prose)
        run: |
          GIT_SSH_COMMAND="ssh -i ~/.ssh/trader_key -o IdentitiesOnly=yes" \
            git clone -q --branch claude/arena git@github.com:TomHaramaty/trader-arena.git trader
          git -C trader config user.name "arena-bot"
          git -C trader config user.email "arena-bot@users.noreply.github.com"
      # A brain is the one thing in this workflow that runs on somebody else's
      # machine, and on 2026-07-31 it refused us for three hours: the managed
      # agent answered 403 to every session. Because this step had no guard,
      # each failure also skipped everything below it — new principals went
      # unseated, the floor froze mid-afternoon, and the record had to be
      # published by hand. Twice. A model outage must cost the arena its
      # sessions and nothing else, so every step below runs whatever happens
      # here. NOT continue-on-error: that displays a genuinely failed step as
      # green in the API and the UI, which is how an outage becomes invisible.
      # The wake stays red, the tick's conclusion stays red, and the record is
      # written and published anyway.
      - name: Wake triggered agents
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TRADER_REPO: ${{ github.workspace }}/trader
          GIT_SSH_COMMAND: ssh -i ~/.ssh/trader_key -o IdentitiesOnly=yes
        run: python -m jobs.dispatch --triggers-only
      # Everything from here writes or publishes the record, so each one runs
      # whatever happened above it. `if: always()` and not `success()`: these
      # steps are the point of the tick.
      - name: Ingest seat applications
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TRADER_REPO: ${{ github.workspace }}/trader
          GIT_SSH_COMMAND: ssh -i ~/.ssh/trader_key -o IdentitiesOnly=yes
          FIRESTORE_SERVICE_ACCOUNT: ${{ secrets.FIRESTORE_SERVICE_ACCOUNT }}
          # the founder's welcome goes out at seating (jobs/welcome.py)
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: |
          printf '%s' "$FIRESTORE_SERVICE_ACCOUNT" > "$RUNNER_TEMP/fs_sa.json"
          GOOGLE_APPLICATION_CREDENTIALS="$RUNNER_TEMP/fs_sa.json" python -m jobs.ingest
      - name: Sync who is named on the floor
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FIRESTORE_SERVICE_ACCOUNT: ${{ secrets.FIRESTORE_SERVICE_ACCOUNT }}
        run: |
          printf '%s' "$FIRESTORE_SERVICE_ACCOUNT" > "$RUNNER_TEMP/fs_sa.json"
          GOOGLE_APPLICATION_CREDENTIALS="$RUNNER_TEMP/fs_sa.json" python -m jobs.credit
      - name: Take guidance from the desks
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TRADER_REPO: ${{ github.workspace }}/trader
          GIT_SSH_COMMAND: ssh -i ~/.ssh/trader_key -o IdentitiesOnly=yes
          FIRESTORE_SERVICE_ACCOUNT: ${{ secrets.FIRESTORE_SERVICE_ACCOUNT }}
        run: |
          printf '%s' "$FIRESTORE_SERVICE_ACCOUNT" > "$RUNNER_TEMP/fs_sa.json"
          GOOGLE_APPLICATION_CREDENTIALS="$RUNNER_TEMP/fs_sa.json" python -m jobs.guidance
      - name: Build arena.json
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TRADER_REPO: ${{ github.workspace }}/trader
        run: python -m jobs.site
      - name: Push data to arena-web
        if: always()
        env:
          ARENA_WEB_SSH_KEY: ${{ secrets.ARENA_WEB_SSH_KEY }}
        run: |
          bash tools/publish_arena.sh "data: tick refresh"
      # Last, so a finding can never stop the record being written or published.
      # Exits non-zero only on an ERROR — a book that does not match its fills, a
      # journal stranded by a failed push, an entry missing from the record, a
      # condition waking a brain in a loop. Those need a person. Everything else
      # prints and passes.
      - name: The arena checks itself
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TRADER_REPO: ${{ github.workspace }}/trader
        run: python -m jobs.doctor
